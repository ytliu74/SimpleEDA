#if !defined(UTILS_H)
#define UTILS_H

#include <math.h>
#include <iostream>
#include <vector>
#include <QString>
#include <QRegExp>

void printQString(const std::string comment, const QString qstring);

#endif // UTILS_H
