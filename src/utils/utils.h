#if !defined(UTILS_H)
#define UTILS_H

#include <math.h>

#include <QRegExp>
#include <QString>
#include <iostream>
#include <vector>

void printQString(const std::string comment, const QString qstring);

#endif  // UTILS_H
